import Auth from '../utils/auth';
import { Container } from 'react-bootstrap'

const HomePage = () => {

    return (
        <Container>
        <h1>Welcome to Dokay</h1>
        </Container>
    )
}

export default HomePage;